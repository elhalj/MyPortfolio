import React from "react";

// This component is intentionally a no-op. Next.js App Router should use the
// Metadata API in `src/app/layout.tsx` and page files for SEO.
// Keeping this placeholder avoids type errors from the previous `react-helmet`
// usage while preventing runtime effects.
const SEO: React.FC = () => null;

export default SEO;
